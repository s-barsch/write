

    /*
  handleBlur(f) {
    const local = this.state.local.slice();
    for (let s of local) {
      if (s.path === f.path) {
        s = f;
      }
    }
    this.setState({
      files: this.state.files,
      local: local,
    });
  }
  */

/*
function Info(props) {
  return (
    <div className="info">
    <span className="name-label">Name:</span>
    <input className="name" type="text" value={props.value}
    onBlur={props.onBlur}
    onChange={props.onChange} />
    </div>
  )
}

function TextDisplay(props) {
  return (
    <pre key={props.i}>{props.file.body}</pre>
  )
}
*/


